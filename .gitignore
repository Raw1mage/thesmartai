.DS_Store
node_modules
.worktrees

# Per-conversation upload landing zone (specs/docx-upload-autodecompose).
# docxmcp decomposes docx into incoming/<stem>/ subtrees here; legacy OLE2
# fallback also lives under this dir. Never tracked.
/incoming/
.sst
.env
.idea
.vscode
*~
playground
tmp
typecheck.log
dist
ts-dist
.turbo
**/.serena
.serena/
/result
# refs — tracked as submodules, don't ignore
# refs
Session.vim
opencode.json
/config/opencode/
/templates/opencode.json
/templates/backup/
a.out
target
.scripts
.direnv/

# Claude Code session data
.claude/

# Local dev files
opencode-dev
logs/
*.bun-build
tsconfig.tsbuildinfo
__pycache__/
*.py[cod]

# config/opencode/ contains both shared config and sensitive credentials
# Ignore only the auth/account files
/config/opencode/*-accounts.json
/config/opencode/mcp-auth.json

# machine-local state (path usage telemetry)
/config/state/frecency.jsonl

# runtime/local machine artifacts (cms workflow: template-first commits)
/config/data/*.json
/config/state/*.json
/config/state/*.jsonl
/config/lib/librust_pty.so
/config/lib/librust_pty_arm64.so

# local build output logs
/build.log
/recyclebin/
daemon/opencode-gateway
.opencode/mcp-output/

# beta XDG isolation env (per worktree, never commit)
.beta-env/
