#### 功能：系統清理與整合

**需求**

- 移除冗餘的 `DialogModelHealth` 組件，確認其功能已整合至 `DialogAdmin`。
- 釐清 "cli" 使用者來源。

**範圍**

- IN：移除 `src/cli/cmd/tui/component/dialog-model-health.tsx`。
- OUT：修改 "cli" 標記邏輯（除非使用者明確要求隱藏，目前僅作解釋）。

**方法**

- 刪除冗餘檔案。
- 確認無其他引用。

**任務**

1. [ ] 刪除 `src/cli/cmd/tui/component/dialog-model-health.tsx`。
2. [ ] 驗證 `DialogAdmin` 的 Activities 分頁功能完整。

**待解問題**

- 無。

#### 功能：antigravity 逐帳號配額顯示（Activities + Model Select）

**需求**

- Model Activities 顯示 antigravity 各帳號的獨立用量。
- Providers/Model Select 同步使用逐帳號用量。
- 若無配額資料，顯示 `--`。

**範圍**

- IN：更新 antigravity quota 查詢結果的對應方式（以帳號維度），並改用該映射顯示。
- OUT：不改變其他 provider 的用量邏輯。

**方法**

- 以 core account 的 refreshToken/email 對應 antigravity quota 結果，建立 `accountId -> quota groups` 映射。
- Activities 與 Model Select 皆改用該映射查詢配額。
- 無配額時回傳 `--`。

**任務**

1. [x] 產生 `accountId -> quota groups` 映射並接入 Activities。
2. [x] 同步 Providers/Model Select 的配額顯示邏輯。
3. [x] 更新 `docs/DIARY.md` 索引。

**待解問題**

- 無。

#### 功能：移除 Model Activities 用量「…」placeholder

**需求**

- 在 Admin Panel > Model Activities 列表中，取消顯示「…」placeholder。
- 對非 OpenAI（無用量值）不顯示任何多餘字樣。

**範圍**

- IN：調整 Activities 列表欄位寬度與標題字串長度。
- OUT：不更動其他頁面（Favorites / Providers）或全域 `DialogSelect` 行為。

**方法**

- 將 Activities 列表的 Model 欄位寬度縮減 1 字元，避免 `Locale.truncate` 截斷出現「…」。
- 同步調整 header 列字串長度，維持欄位對齊。

**任務**

1. [x] 調整 Activities 標題欄位寬度（model column -1）。
2. [x] 同步更新 header 列字串長度。
3. [x] 更新 `docs/DIARY.md` 索引。

**待解問題**

- 無。

#### 功能：rotation3d fallback 與 TUI 模型同步

**需求**

- TUI prompt 模型顯示需反映 rotation3d fallback 的實際模型。
- rotation3d fallback 需排除使用者已隱藏的模型與提供者。

**範圍**

- IN：prompt 模型同步、rotation3d 候選篩選、DIARY 索引。
- OUT：新增 SDK 事件型別或後端狀態欄位。

**方法**

- 以最新 assistant 訊息的 providerId/modelID 同步本地模型狀態（避免顯示與實際模型不同步）。
- 讀取 model.json 的 hidden/hiddenProviders，將其排除在 rotation3d fallback 候選之外。

**任務**

1. [x] prompt 同步 assistant 訊息模型。
2. [x] rotation3d 排除 hidden 模型與提供者。
3. [x] 更新 docs/DIARY.md 索引。

**待解問題**

- 無。
