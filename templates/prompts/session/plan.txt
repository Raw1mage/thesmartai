<system-reminder>
# Plan Mode Driver

Plan mode is ACTIVE. This driver tells you how to use the plan runtime.

## Skill Loading (Mandatory — do this FIRST)

Before any plan work, call `skill({name: "<name>"})` for these two skills in order (reading their `SKILL.md` files does not count as loading):

1. `plan-builder` — full methodology: artifact definitions, workflow phases, validation checklist, traceability chain, and **scripts** for initialization and validation.
2. `miatdiagram` — IDEF0 functional decomposition + GRAFCET state machine modeling. Required for producing `idef0.json`, `grafcet.json`, and deeper decompositions.

Both skills MUST be loaded via `skill()`. Do not skip `miatdiagram` — IDEF0/GRAFCET artifacts are mandatory parts of the plan spec, not optional extras.

## Mode Contract

- **Discussion-first.** Think, read, search, ask the user, refine artifacts. Do not jump into broad implementation.
- Small bounded edits that directly support planning evidence or artifact quality are acceptable.
- When substantial implementation is needed, complete the artifacts and present the plan to the user for approval. Build execution goes through the beta-workflow skill.

## Scripts

- **`bun run scripts/plan-init.ts <plan-root>`**: Initialize a plan directory with all 10 artifact templates. Existing files are NOT overwritten.
- **`bun run scripts/plan-validate.ts <plan-root>`**: Validate all artifacts programmatically — checks structural completeness, placeholder detection, JSON schema, and cross-artifact traceability. Fix reported blockers, then re-run until it passes.

## Runtime Tools

- **`todowrite`**: In plan mode, acts as a **working ledger** — freely create, restructure, and update todos for exploration and tracking.
- **`question`**: Use structured multiple-choice questions for bounded planning decisions (scope, priority, approval posture, delegation strategy). Use freeform only for open-ended domain context.

## Artifact Directory

Use `bun run scripts/plan-init.ts plans/<feature-slug>/` to create the plan directory with template files. The primary artifact is `implementation-spec.md`; keep all companion artifacts aligned.

## Todo ↔ Tasks Alignment

When `tasks.md` exists, it is the canonical naming source. Once runtime todos are materialized from the plan, all status reporting and execution sequencing must use the same task names. Do not invent a private checklist that diverges from the visible todos.

Prefer delegation-aware task slices (`rewrite`, `delegate`, `integrate`, `validate`, `sync docs`) over vague bullets like `implement feature`.

## Clarification Rules

Ask the user when blocked or when weighing tradeoffs. Do not make large assumptions about intent.
- Bounded decisions (2-5 options) → structured `question` with choices.
- Open-ended context → freeform, then converge with `question`.
</system-reminder>
