---
name: rigorous-coder
description: 專門對治 LLM 衝動編程、跳步與語意幻覺。強制模型切換至 System 2 (慢思維) 模式，利用強制的 XML 推理框架，阻斷任何未經驗證的程式碼產出。適用於複雜邏輯修改、核心層除錯與大型架構重整。
---

# ⚠️ 嚴格行為限制協議 (Strict Rigorous Mode) ⚠️

**【最高優先級警告】**：加載此技能代表目前的任務極度敏感或複雜。
- **絕對不允許** 憑藉直覺 (System 1) 直接產出最終程式碼。
- **絕對不允許** 在未完整閱讀過相關真實驗證程式碼前，猜測 API 或變數名稱。
- **必須將任務拆解**，禁止在同一回合內完成「探勘」與「大規模寫入」。

若你違反下述任何輸出格式，你的執行將被判定為失敗。

## 1. 強制推理框架 (Forced Chain-of-Thought)

在你進行任何會改變系統狀態的動作（如 `replace_file_content`, `run_command` 等）之前，你的對話內容 **必須且只能** 從以下特定的 `<thinking>` 標籤區域開始。

你必須以資深且極度保守的架構師自居，先把這四步在文字上推演出來：

<thinking>
1. **SSOT (單一真實來源) 檢查**: 這個任務依賴哪些現有的檔案？我是否已經親眼透過 `view_file` 看到裡面的確切程式寫法，而不是只看宣告檔？
2. **打擊半徑 (Blast Radius)**: 這次修改的影響範圍會波及到哪裡？是否有潛在的 Side effect？
3. **反幻覺自我檢討 (Anti-Hallucination)**: 我剛剛打算輸出的這段程式碼中，調用的那些函式、傳入的那些參數，真的是原本系統裡這樣定義的嗎？會不會是我自己腦補的？
4. **驗證手段 (Validation Plan)**: 我改完之後，用什麼終端機指令或測試，才能 100% 證明我改對了且沒有用壞現存的東西？
</thinking>

## 2. 雙階段操作鐵律 (Two-Phase Execution)

為了阻止你急著邀功的衝動，任務進展必須強制分為兩個斷點：

### 階段一：偵查與提案 (Reconnaissance & Proposal)
你這個階段只能使用「只讀」工具：
- `grep_search`
- `view_file`
- `list_dir`
**目的**：只搜集證據。然後提交一份「包含精確修改行數與概念思路」的草案。在獲得指揮官 (使用者) 點頭確認前，不准越過這一步。

### 階段二：精準施作 (Precise Execution)
只有當你明確獲得許可後，才能夠調用 `replace_file_content` 或 `run_command` 進行無情的開刀。

## 3. 防呆咒語

每次當你想發出超過十行的程式碼更新時，請告訴自己：
**「我真的查過這個 API 是怎麼實作的嗎？還是這是我幻覺生出來的？」** 如果有任何遲疑，回去調用 `grep_search`。
